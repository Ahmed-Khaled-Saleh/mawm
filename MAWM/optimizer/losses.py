"""Contain various loss functions used for optimization."""

# AUTOGENERATED! DO NOT EDIT! File to edit: ../../nbs/04_optimizer_losses.ipynb.

# %% auto 0
__all__ = []

# %% ../../nbs/04_optimizer_losses.ipynb 3
from fastcore import *
from fastcore.utils import *

# %% ../../nbs/04_optimizer_losses.ipynb 4
import torch
import torch.nn as nn
import torch.nn.functional as F

# %% ../../nbs/04_optimizer_losses.ipynb 5
def _reward_loss(reward_dist, rewards):
    reward_loss = -torch.mean(reward_dist.log_prob(rewards))
    return reward_loss

# %% ../../nbs/04_optimizer_losses.ipynb 6
def _vae_loss(recon_x, x, mu, logsigma):
    """ VAE loss function """
    BCE = F.mse_loss(recon_x, x, reduction='sum')
    KLD = -0.5 * torch.sum(1 + 2 * logsigma - mu.pow(2) - (2 * logsigma).exp())
    return BCE + KLD

# %% ../../nbs/04_optimizer_losses.ipynb 7
def _l1_loss(z, h, loss_exp=1.0):
    return torch.mean(torch.abs(z - h) ** loss_exp) / loss_exp
